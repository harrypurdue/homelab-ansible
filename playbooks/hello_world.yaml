- name: Homelab Ubuntu Hello World
  gather_facts: false
  hosts: all

  tasks:

    - name: gather facts
      ansible.builtin.setup:
        filter: distribution_release
      register: abc

    - name: display gathered facts
      ansible.builtin.debug:
        msg: "{{ abc }}"