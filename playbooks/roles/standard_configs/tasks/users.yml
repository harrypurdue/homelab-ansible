---
- name: ansible user
  ansible.builtin.user:
    name: "{{ item.name }}"
    state: present
    password: "{{ vars[item.name + '_local_user_pass'] }}"
    groups: "{{ default_groups }}"
    append: false
    shell: /bin/bash
  become: true
  with_items: "{{ users }}"
