#!/usr/bin/env -S ansible-playbook -i inventory/netbox.yml -i inventory/proxmox.yml
---
- name: updates in dev
  hosts: tags_dev
  strategy: free

  roles:
    - role: updates

- name: updates in prod
  hosts: tags_prod
  strategy: free

  roles:
    - role: updates
