ubuntu_dns_servers:
    hosts:
        dns-0[1:2].lab.lan:
    vars:
        verification: dns.yaml

ubuntu_staging_servers:
    hosts:
        staging-0[1:1].lab.lan:
    vars:
        verification: staging.yaml

ubuntu_zabbix_servers:
    hosts:
        zabbix-01.lab.lan:
    vars:
        verification: zabbix.yaml

ubuntu_docker_servers:
    hosts:
        docker-0.lab.lan:
    vars:
        verification: docker.yaml

ubuntu_certificate_authority_servers:
    hosts:
        ca-01.lab.lan:
    vars:
        verification: ca.yaml

ubuntu_dhcp_servers:
    hosts:
        dhcp-01.lab.lan:

test_servers:
    children:
        ubuntu_staging_servers:
    hosts:        
        dns-02.lab.lan:
        staging-0[1:1].lab.lan:

prod_servers:
    hosts:
        dns-01.lab.lan:
        zabbix-01.lab.lan:
        docker-01.lab.lan:
        ca-01.lab.lan:
        dhcp-01.lab.lan:

homelab_ubuntu_servers:
    children:
        ubuntu_dns_servers:
        ubuntu_staging_servers:
        ubuntu_zabbix_servers:
        ubuntu_docker_servers:
        ubuntu_certificate_authority_servers:
        ubuntu_dhcp_servers:

all_ubuntu_servers:
    children:
        homelab_ubuntu_servers:
    vars:
        ansible_user: ansible
        ansible_password: uJJqrcag86RPuGUFMmNHhi7YYubzbHNoAbfddNauCVHaG7BAsQYRbV6DYPX38gUX
        ansible_become_password: uJJqrcag86RPuGUFMmNHhi7YYubzbHNoAbfddNauCVHaG7BAsQYRbV6DYPX38gUX