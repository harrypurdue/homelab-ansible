# DHCP Servers
There are two DHCP servers. One in the `production` environment and one in the `dev` environment.  

To install and configure the DHCP servers, run the following playbook.

`./dhcp_servers.yml`

This playbook will install the dhcp server package onto dhcp servers and configure the server.  